DB_PROVIDER=sqlite
DATABASE_URL="file:./dev.db"
# For Postgres (prod):
# DB_PROVIDER=postgresql
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DB?sslmode=require"

# Worker schedule (cron, optional if using node-cron intervals)
FETCH_INTERVAL_SECONDS=30
